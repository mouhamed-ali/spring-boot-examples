spring:
  profiles:
    active: tomcat
    #active: dbcp
  datasource:
    initialization-mode: always
    url: jdbc:h2:mem:testdb;DB_CLOSE_ON_EXIT=FALSE
    username: postgres
    password: password
